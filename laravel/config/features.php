<?php

return [

    /*
    |--------------------------------------------------------------------------
    | CRM API
    |--------------------------------------------------------------------------
    |
    | Бэкенд CRM (компании, сотрудники, контрагенты, заявки) лежит в том же
    | приложении, что и сайт, но своего фронта пока не имеет. Пока он не готов,
    | роуты /api/* не регистрируются вообще — иначе на прод уехали бы открытые
    | без токена /api/register и /api/dadata/search (последний тратит платную
    | квоту Дадаты).
    |
    | Локально ставим CRM_API_ENABLED=true, на проде переменной просто нет.
    |
    */

    'crm_api' => (bool) env('CRM_API_ENABLED', false),

];
